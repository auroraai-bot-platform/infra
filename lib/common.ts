import { DefaultRepositories } from '../types';

export const defaultRepositories: DefaultRepositories = {
  actionsRepository: 'actions-private',
  botfrontRepository: 'botfront-private',
  rasaBotRepository: 'rasa-private',
};

export const domain = 'aaibot.link';